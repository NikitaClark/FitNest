<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Background color of EditText -->
    <stroke android:width="1dp" android:color="@color/white" /> <!-- Outline color -->
    <corners android:radius="4dp" /> <!-- Optional: if you want rounded corners -->
</shape>